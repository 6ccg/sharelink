# Required. First admin password for an empty database.
INITIAL_ADMIN_PASSWORD=change-me-promptly

# Required. Use a random string with at least 32 characters.
JWT_SECRET=sharelink-session-secret-key-32-chars

# Optional. Uncomment only when you need to change the public host port.
# HOST_PORT=8080
